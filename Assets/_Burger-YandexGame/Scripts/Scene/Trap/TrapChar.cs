using DG.Tweening;
using UnityEngine;

public class TrapChar : Trap
{
    //[SerializeField] private Vector3 startAnimation;
    //[SerializeField] private Vector3 endAnimation;

    //private void Start()
    //{
    //    transform.eulerAngles = startAnimation;

    //    transform.DORotate(new Vector3(endAnimation.x, startAnimation.y, startAnimation.z), rotateSpeed, RotateMode.FastBeyond360)
    //        .SetEase(Ease.Linear)
    //        .SetLoops(-1, LoopType.Yoyo);
    //}
}
